name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build -t meenatchi2007/aws-cicd:${{ github.sha }} .
          docker tag atomcodex/aws-cicd:${{ github.sha }} atomcodex/aws-cicd:latest

      - name: Push Docker Images
        run: |
          docker push meenatchi2007/aws-cicd:${{ github.sha }}
          docker push meenatchi2007/aws-cicd:latest

  deploy:
    needs: build-and-push
    runs-on: self-hosted

    steps:
      - name: Pull Latest Docker Image
        run: |
          sudo docker pull meenatchi2007/aws-cicd:latest

      - name: Stop Existing Container
        run: |
         sudo docker stop aws-cicd-container || true
         sudo docker rm aws-cicd-container || true

      - name: Run New Container
        run: |
          sudo docker run -d \
            --name aws-cicd-container \
            -p 80:80 \
            --restart always \
            meenatchi2007/aws-cicd:latest
